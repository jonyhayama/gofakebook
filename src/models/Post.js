class Post {
  constructor({
    id, content, createdAt, author,
  }) {
    this.id = id;
    this.content = content;
    this.createdAt = createdAt;
    this.author = author;
  }
}

export default Post;
